<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog[0].title %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7f6;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        
        /* Container for the blog post */
        .post-container {
            max-width: 900px;
            background-color: #ffffff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            line-height: 1.8;
        }

        /* Title styling */
        h1 {
            font-size: 2.5rem;
            color: #2c3e50;
            margin-bottom: 20px;
            font-weight: 700;
        }

        /* Metadata styling */
        .post-meta {
            font-size: 1rem;
            color: #95a5a6;
            margin-bottom: 30px;
            font-style: italic;
        }

        /* Blog content styling */
        .post-content {
            font-size: 1.2rem;
            color: #2c3e50;
            letter-spacing: 0.5px;
            line-height: 1.8;
            margin-bottom: 30px;
            white-space: pre-line;
        }

        /* Image styling */
        .post-image {
            max-width: 100%;
            height: auto;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        /* Back button styling */
        a.back-button {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        /* Back button hover effect */
        a.back-button:hover {
            background-color: #0056b3;
        }

        /* Responsive typography for smaller screens */
        @media screen and (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }

            .post-content {
                font-size: 1rem;
            }

            .post-container {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="post-container">
        <h1><%= blog[0].title %></h1>
        <p class="post-meta">Category: <%= blog[0].categoryName %></p>

        <!-- Display the image if it exists -->
        <% if (blog[0].imageUrl) { %>
            <img src="/<%= blog[0].imageUrl %>" alt="Blog Image" class="post-image">
        <% } %>

        <div class="post-content">
            <%= blog[0].content %>
        </div>
        <!-- <a href="/api/blogs/all" class="back-button">Back to All Posts</a> -->
    </div>
</body>
</html>
